---
- name: "List Existing User "
  ignore_errors: no
  os_project_facts:
    name: "{{ admin_project_name }}"
    auth:
      auth_url: "{{ auth_url }}"
      username: "{{ username }}"
      password: "{{ password }}"
      project_name: "{{ project_name  }}"
    cacert: "{{ cacert }}"
- debug:
    var: openstack_projects
